# Copy to `keystore.properties` (gitignored) for LOCAL signed release builds.
# In CI these are provided as environment variables from GitHub secrets instead
# (see .github/workflows/release.yml) — do not commit the real file or keystore.
storeFile=release.keystore
storePassword=CHANGE_ME
keyAlias=nearaid
keyPassword=CHANGE_ME
